name: Tag 2

on:
  push:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: node tag-and-mention.mjs スパムを一括で消せるタグ
        env:
          MASTODON_HOST: ${{ secrets.MASTODON_HOST }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
